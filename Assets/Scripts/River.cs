using UnityEngine;

public class River : MonoBehaviour
{
    // This script is now optional — it just serves as a tag holder or future extension.
    // The tag "River" is still important for RiverEffect.cs to detect.

    // You may leave this empty or use it later to trigger VFX, splash sounds, etc.
}
